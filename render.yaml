services:
  - type: web
    name: inst-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 2 --timeout 180
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true
